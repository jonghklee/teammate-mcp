{
  "_comment": "Drop this into ~/.claude/settings.json (merge with your existing config). Adjust the command path if you installed teammate-mcp somewhere unusual.",

  "mcpServers": {
    "teammate": {
      "command": "teammate-mcp",
      "args": ["serve"],
      "env": {
        "TEAMMATE_QUEUE_MODE": "ephemeral"
      }
    }
  },

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "teammate-mcp-hook-block-infinite-poll" }
        ]
      }
    ],
    "_comment_Stop": "Deterministic ping-pong: on turn end, if this pane has pending teammate mail, block the stop and process it (loop-safe via stop_hook_active). bin/install-claude wires this automatically; adjust the path if installed elsewhere. Append alongside any existing Stop hooks (e.g. a notify hook).",
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "python3 ~/programming/teammate-mcp/hooks/stop_inbox_drain.py" }
        ]
      }
    ]
  },

  "permissions": {
    "allow": [
      "mcp__teammate__ask_codex",
      "mcp__teammate__ask_claude",
      "mcp__teammate__broadcast",
      "mcp__teammate__queue_status"
    ]
  }
}
