---
description: "Register THIS pane in teammate-mcp under an auto-assigned label."
---

Call `mcp__teammate__register_self` with no `label` argument (or with
the label the user supplied if any). The server picks the next free
`{job}{n}` slot — `claude1`, `codex1`, `codex2`, etc.

Reply with one short line: `✓ registered as <label>`. Then end the
turn. Do not call any other tool.
